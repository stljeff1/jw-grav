
        <div class="top-nav" data-spy="affix" data-offset-top="1">
        

            <div class="navigation-container">

                <!-- Collect the nav links, forms, and other content for toggling -->
                <nav class="navbar navbar-dark navbar-expand-lg">


                    <div class="site-name-container navbar-brand">

                        <h1 class="site-name">
                            <a class="" href="{{ home_url }}">
                                Jeff Wilkerson

                            </a>
                        </h1>
                    </div>
                    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#jw-default-nav" aria-controls="jw-default-nav" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>

                

                    <div class="navbar-list-container collapse navbar-collapse navbar-right" id="jw-default-nav">
                        <ul class="navbar-nav">
                            {% if theme_config.dropdown.enabled %}
                                {{ _self.loop(pages) }}
                            {% else %}
                                {% for page in pages.children.visible %}
                                    {% set current_page = (page.active or page.activeChild) ? 'active' : '' %}
                                    <li class="nav-item {{ current_page }}">
                                        <a class="nav-link" href="{{ page.url }}">
                                            {% if page.header.icon %}<i class="fa fa-{{ page.header.icon }}"></i>{% endif %}
                                            {{ page.menu }}
                                        </a>
                                    </li>
                                {% endfor %}
                            {% endif %}
                            {% for mitem in site.menu %}
                                <li class="nav-item">
                                    <a href="{{ mitem.url }}" class="nav-link">
                                        {% if mitem.icon %}<i class="fa fa-{{ mitem.icon }}"></i>{% endif %}
                                        {{ mitem.text }}
                                    </a>
                                </li>
                            {% endfor %}
                            {% if config.plugins.login.enabled and grav.user.username %}
                                <li class="nav-item"><i class="fa fa-lock"></i> {% include 'partials/login-status.html.twig' %}</li>
                            {% endif %}    

                        </ul>
                    </div>


                </nav><!-- /.navbar-collapse -->
                

            </div>
        </div>