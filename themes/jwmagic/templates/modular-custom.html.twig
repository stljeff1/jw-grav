{% extends 'base/modular-base.html.twig' %}


{% block main %}
    {{ page.content }}
    {% for module in page.collection() %}
        
        <section id="{{ module.slug }}" class="{{ module.header.body_classes}}">
            <div class="container">

                <!-- START module.conent -->
                
                {{ module.content }}

                <!-- END module.content -->
            </div>
        </section>
    {% endfor %}
{% endblock %}
